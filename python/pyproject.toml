[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "xfingine"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: Apache Software License",
    "Topic :: Office/Business :: Financial",
]
dynamic = ["version"]
description = "Pure computation engines for personal finance planning - inflation-adjusted EMI schedules and more"
readme = "README.md"

[project.urls]
Homepage = "https://github.com/xfina-dev/xfingine"
Repository = "https://github.com/xfina-dev/xfingine"

[tool.maturin]
features = ["pyo3/extension-module"]
# One abi3 wheel per OS/arch covers CPython 3.8+, instead of one per version.

module-name = "xfingine"
